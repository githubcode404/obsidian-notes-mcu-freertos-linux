|                | 静态分配                 | 动态分配            |
| -------------- | -------------------- | --------------- |
| **时机**         | 编译时                  | 运行时             |
| **内存来源**       | 全局/静态数组（.bss/.data段） | 堆（Heap）         |
| **是否可能失败**     | 不会                   | 会（堆耗尽）          |
| **内存回收**       | 不能（除非整个程序结束）         | 可以（free/delete） |
| **内存碎片**       | 无                    | 有               |
| **确定性**        | ✅ 高（实时系统要求）          | 较低              |
| **灵活性**        | 低                    | ✅ 高             |
| **FreeRTOS函数** | `xTaskCreateStatic`  | `xTaskCreate`   |

heap2：空间释放后不会合并，导致碎片化
heap4：可合并相邻空闲空间
heap5：可管理零散空间（比如RAM和

