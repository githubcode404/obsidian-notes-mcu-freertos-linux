**任务通信**（人物之间传输数据）
![[Pasted image 20260520083421.png]]

**全局变量**：
- 多个任务共用一个变量
- 变量修改可分为三个原子操作，可能被打断
  1. 全局变量写入寄存器
  2. 寄存器修改值
  3. 寄存器写入全局变量


**环形缓冲区**：类似环形队列，适用**两个任务**
![[Pasted image 20260520084220.png]]**w：当前要写的位置**
1. 判断是否满（next_w == r）
2. 未满，写入，w++
3. 判断是否溢出
4. 修改next_w，判断是否溢出

**r：当前要读的位置**
1. 判断是否空（r == w）
2. 非空，r++
3. 判断r是否溢出

读写两个任务**只使用w或者r一个变量**，实现无差错通信

**为什么不用全局变量num记录环形缓冲区数据个数**
- 这种方法本质还是使用全局变量进行任务通信，任务切换可能打断num修改操作，导致混乱


## 8-2
**队列本质**：环形Buf+两个链表（sender_list, receive_list),
- 能实现**保护**，**阻塞+唤醒**的环形Buf
- 数据被读后会从队列**删除**
**读数据**
1. 队列空，任务加入接收链表，从就绪队列转移到阻塞队列
2. 若阻塞中断前被唤醒（有数据写入），则进入就绪队列，离开接收链表，接收数据
3. 否则到时间后进入就绪列表，返回错误值

**写数据**
1. 队列满，任务加入发送链表，从就绪队列转移到阻塞队列
2. 若阻塞中断前被唤醒（有数据被读出），则进入就绪队列，离开发送链表，接收数据
3. 否则到时间后进入就绪列表，返回错误值

**遥控器控制**
- 原代码读写环形缓冲区，使用轮询等待读写，cpu利用率低
- 改为队列，阻塞+唤醒
修改逻辑
- 游戏任务开始处创建队列（提前创建句柄，队列内数据类型）
- 遥控器接收中断回调函数：数据写入队列
- 控制挡板函数：读队列
TIP：
- 中断和任务里面读写队列函数API不同
- 中断函数不允许等待，否则会影响其他中断和任务


**编码器控制**
静态创建队列
- 参数：xQueueCreateStatic：记录队列状态（队列长度，数据大小，读写指针）
流程
- 编码器ISQ函数写编码器队列
- 游戏任务开始处创建编码器处理数据的任务
- 编码器任务
   - 读编码器队列
   -  处理数据
   - 写反弹板控制队列



## 队列集
