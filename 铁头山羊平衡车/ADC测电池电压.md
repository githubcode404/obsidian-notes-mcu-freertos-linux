中断：IRQ

中断号（IRQn)：中断号的核心作用是 **“定位中断服务函数”**（解决 “找谁做”），而 “触发中断后要做什么”，是由你在该中断号对应的 **中断服务函数中编写的代码决定** 的。

怎么查：stm32f10x.h ctrl+f 找IRQ 
抢占优先级>子优先级 (数值越小越高)

中断响应函数查找：startup_stm32f10x_md.s
DCD + 中断响应函数

总结：
使用定时器作为外部触发源，单通道ADC测电压，读取数据


**整体逻辑**：定时器产生周期性触发信号 -> 触发ADC注入通道采样 -> 采样完成触发JEOC中断 -> 中断中读取采样值并换算

1.**配置定时器**
初始化对应引脚模式af 
开启定时器时钟
配置时基单元
选择从模式控制TRGO
使能定时器

2.**ADC初始化**
配置时钟分频（最大14MHZ）
配置IO接口（AIN 模拟输入）
开启ADC时钟
配置ADC参数（连续采样，对齐方式，外部触发源（默认是配置常规序列的），模式，通道数，扫描模式）

**注入序列**
配置外部触发源（注入序列通过外部触发）
使能外部触发
配置注入通道（哪个通道，采样周期

**配置JEOC中断**
使能JEOC中断
配置NVIC
（中断优先级分组（放main里面，整个项目只配置一次）NVIC参数（中断号，抢占优先级和子优先级设置，使能通道））
使能ADC

**中断函数**（通过查找明确函数名称
中断标志位判断（明确是注入完成产生的中断）+清零
读取诸如通道的采样值（12位）
转换为实际电压




